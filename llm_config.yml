providers:
  primary_gemini:
    type: "gemini"
    base_url: "https://zhiuai.com/v1"
    model: "gemini-3-pro-preview"
    api_key_env: "GEMINI_API_KEY"


chains:
  default:
    - provider: "primary_gemini"
      timeout_s: 20

  idea_expand:
    - provider: "primary_gemini"

  research:
    - provider: "primary_gemini"

global:
  max_retries_per_provider: 1
  overall_timeout_s: 60
